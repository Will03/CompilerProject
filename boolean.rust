// variables
let mut c;
let mut a = 5;
// function declaration
fn add(a:int, b:int) -> int {
return a+b;
}
// main function
fn main() {
    c = add(a, 10);
    if (c > 10) {
    println -c;
        if (c > 10) {
            println "fuck";
        }
    }
    else 
    {
        print c;
    }
    println "Hello World";
}